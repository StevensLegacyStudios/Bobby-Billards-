// Shared financial assumptions. Tweak per market.

/** Sacramento, CA combined sales tax (~8.75%). */
export const DEFAULT_SALES_TAX = 0.0875;

/** Typical dealer doc + title + registration fees rolled into a deal. */
export const DEFAULT_FEES = 600;

/** Default loan term used for payment estimates. */
export const DEFAULT_TERM_MONTHS = 72;

/** California regular gas price assumption ($/gal). */
export const DEFAULT_GAS_PRICE = 4.8;
